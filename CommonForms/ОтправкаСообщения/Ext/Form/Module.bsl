&НаСервере
&После("ПриСозданииНаСервере")
Процедура А1Емэйл_ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	//Не работает т.к. параметр имеет тип "Строка". ТУДУ: сделать чтобы работало.
	Если ТипЗнч(Параметры.Текст) = Тип("Структура") Тогда
		А1Э_ФорматированныйДокумент.УстановитьНТМЛ(ЭтаФорма.ТелоПисьма, Параметры.Текст);
	КонецЕсли;
КонецПроцедуры
